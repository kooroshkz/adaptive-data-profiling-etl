# Core ETL Dependencies
# Python 3.12 compatible versions
apache-airflow==2.10.3        # Workflow orchestration (supports Python 3.12)
duckdb==1.1.3                 # Analytical database
dbt-core==1.8.7               # Data transformation framework
dbt-duckdb==1.8.2             # dbt adapter for DuckDB

# Data Processing
pandas==2.2.3                 # Data manipulation
pyarrow==18.1.0               # Parquet file format support
requests==2.32.3              # HTTP client for API calls

# Utilities
python-dateutil==2.9.0        # Date manipulation
pyyaml==6.0.2                 # YAML configuration files

# Development/Testing (optional but useful)
ipython==8.30.0               # Better Python REPL for testing
