# Python packages required by the Airflow DAGs.
# Installed in the container via _PIP_ADDITIONAL_REQUIREMENTS in docker-compose.yml.

adaptive-profiler   # AutoML anomaly detection (pulls pyod, optuna, scikit-learn, scipy, pyyaml)
duckdb              # local warehouse + parquet queries (transform)
requests            # HTTP requests (Open-Meteo API)
pandas              # data processing in ingestion/scoring scripts
pyarrow             # Parquet read/write
boto3               # OPTIONAL: only used if S3 is configured (S3_BUCKET + AWS creds)
