# Python packages required by Airflow DAGs
# These will be installed in the Docker container on startup

duckdb==1.1.3                 # MotherDuck connection
boto3==1.35.80                # AWS S3 operations
requests==2.32.3              # HTTP requests (API calls, GitHub Actions)
pandas==2.2.3                 # Data processing in ingestion scripts
pyarrow==18.1.0               # Parquet file reading/writing
